// Module.swift — placeholder file for the Statistics target.
//
// The instruction-pack assignments are independent paste-and-run
// programs, not a compilable module. Each `NN-name.swift` file is
// designed to be pasted into the Quiver Notebook editor or run
// individually with `swift /path/to/file.swift`. The files declare
// top-level constants and call top-level `print` and `exit(0)`,
// which is valid for top-level Swift but not for a library module.
//
// This file exists so `swift build` from the package root succeeds
// and Xcode opens the pack as a working Swift Package. The
// `import Quiver` here also resolves the package dependency on first
// build, which is what students benefit from when they open Xcode
// and start editing assignments.
//
// To run an assignment in Xcode: open the file directly, place the
// cursor at the top, and use Editor → Run. To run from the command
// line: `swift Sources/Statistics/01-descriptive-stats-solution.swift`.

import Quiver
